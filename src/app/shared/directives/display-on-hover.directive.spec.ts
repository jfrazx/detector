import { DisplayOnHoverDirective } from './display-on-hover.directive';

describe('DisplayOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
