import { ClickLinkDirective } from './click-link.directive';

describe('ClickLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickLinkDirective();
    expect(directive).toBeTruthy();
  });
});
